# RVTT Agent Rules

이 문서는 RVTT 저장소에서 작업하는 모든 AI 에이전트와 자동화 도구가 따라야 하는 최상위 저장소 규약이다.

목표는 많은 코드를 빠르게 생성하는 것이 아니다. 합의된 제품 방향을 유지하면서 오류를 찾고 수정하기 쉽고, 장시간 세션에서도 안정적으로 동작하는 기능을 검증 가능한 단위로 완성하는 것이다.

---

## 1. 프로젝트의 고정 전제

별도의 새 결정이 있기 전까지 다음을 변경하지 않는다.

1. RVTT는 Roblox에서 D&D 세션을 진행하는 게임형 VTT다.
2. 제품 기준점은 **DM이 실시간으로 진행하는 발더스 게이트형 D&D 세션**이다.
3. 기본 규칙 세트는 `dnd5e-2024`다.
4. 기본 표시 언어는 한국어 `ko-KR`다.
5. 내부 ID, 저장 데이터와 실행 로직은 표시 언어와 분리한다.
6. 캐릭터 시트는 캐릭터 영구 상태와 성장 기록의 원본이다.
7. 캐릭터 생성, 1–20레벨 성장, 직업, 하위직업, 주문, 장비와 전투를 하나의 시스템으로 연결한다.
8. 마법과 특성은 플레이어가 조립하는 범용 프리셋이 아니라 RVTT가 직접 구현하는 정식 콘텐츠다.
9. 추가 공식 서적은 개발자 관리형 출처 팩으로 추가한다.
10. 이동 가능한 표면에는 5피트 × 5피트 논리 격자가 존재한다.
11. 클릭 이동과 WASD 이동은 같은 이동 및 검증 시스템을 사용한다.
12. 중요한 게임 상태와 판정의 최종 권한은 서버가 가진다.
13. 완전 자동화가 부적절한 규칙은 DM 승인과 판정 보조 흐름으로 구현한다.
14. 최적화, 안정성과 클린코드는 모든 기능의 완료 조건이다.

확정된 제품 결정은 `docs/remake/decisions/`의 ADR을 기준으로 한다.

---

## 2. 지시와 문서의 우선순위

저장소 내부 내용이 충돌할 때 다음 순서를 따른다.

1. 사용자가 현재 작업에서 명시적으로 내린 최신 결정
2. 상태가 `확정`인 ADR
3. 승인된 상세 기획 문서
4. 현재 이슈 또는 작업 명세
5. 이 `AGENTS.md`
6. 기존 코드의 관행

이 순서는 저장소와 제품 결정에 적용한다. 플랫폼의 시스템 지시, 보안 정책과 도구 제한보다 우선하지 않는다.

기존 코드가 확정 문서와 충돌하면 기존 코드를 무조건 보존하지 않는다. 충돌을 보고하고 확정 방향에 맞는 변경 범위를 제시한다.

중대한 제품 결정을 임의로 만들지 않는다. 확정 문서끼리 충돌하면 안전하게 진행 가능한 범위까지만 수행하고 충돌을 명시한다.

---

## 3. 기획과 구현을 분리한다

### 기획 작업

- 목표와 비목표를 구분한다.
- 사용자가 실제로 수행하는 흐름을 먼저 정의한다.
- 확정, 제안과 보류를 표시한다.
- 상태, 예외, 실패 흐름과 완료 조건을 적는다.
- 큰 결정은 ADR로 기록한다.
- 구현 세부사항을 제품 요구보다 먼저 확정하지 않는다.

기획만 요청받았는데 프로덕션 코드를 만들지 않는다.

`planning/rvtt-remake`는 리메이크 기획 브랜치다. 사용자가 구현을 명시적으로 요청하지 않았다면 이 브랜치에서 프로덕션 시스템 구현을 시작하지 않는다.

### 구현 작업

- 관련 기획과 ADR을 먼저 읽는다.
- 연결된 기존 코드와 데이터를 조사한다.
- 가장 작은 응집된 기능 단위를 선택한다.
- 서버, 클라이언트, 시트, 저장, 번역, 오류 처리와 검증을 함께 고려한다.
- 문서에 없는 중대한 규칙을 구현 과정에서 몰래 확정하지 않는다.

---

## 4. 작업 시작 절차

모든 작업은 다음 순서로 시작한다.

1. 이 `AGENTS.md`를 읽는다.
2. `docs/remake/README.md`와 관련 ADR을 읽는다.
3. 변경 대상과 연결된 파일을 조사한다.
4. 같은 책임을 가진 모듈, 콘텐츠 ID, 번역 키와 데이터 구조를 검색한다.
5. 작업 범위와 완료 조건을 짧게 정의한다.
6. 현재 브랜치의 기존 변경을 확인한다.
7. 필요한 테스트와 성능 확인 방법을 정한다.

구현·Runtime·Acceptance·검증·Release 관련 작업이면 위 절차와 함께 루트 `AGENT-TEST-STATUS.md`를 반드시 읽고 현재 테스트 상태와 다음 Gate를 확인한다. 테스트 상태가 바뀌는 작업을 수행했다면 같은 작업에서 그 문서도 갱신한다.

파일명이나 모듈 위치를 추측하여 중복 구조를 만들지 않는다.

다른 사람이 만든 변경을 임의로 되돌리거나 덮어쓰지 않는다.

---

## 5. 작업 단위 원칙

### 세로로 완성한다

여러 기능을 얕게 만들지 말고 기능 하나를 실제 사용 가능한 상태까지 연결한다.

예를 들어 직업 특성을 구현한다면 다음을 함께 확인한다.

- 획득과 레벨업
- 시트와 행동 UI
- 사용 조건
- 대상과 판정
- 자원 소비와 회복
- 서버 검증
- 실제 규칙 효과
- 로그와 연출
- 한국어 번역
- 저장과 재접속
- 오류 처리
- 테스트와 성능 영향

### 미완성을 숨기지 않는다

다음 상태를 완성으로 표시하지 않는다.

- 버튼만 있고 실행되지 않음
- 설명만 있고 규칙 효과가 없음
- 클라이언트 연출만 있고 서버 상태가 바뀌지 않음
- 저장되지 않음
- 번역 키가 누락됨
- 잘못된 요청을 검증하지 않음
- 임시 데이터와 상수에 의존함
- 프로파일링하지 않고 최적화됐다고 주장함
- 테스트하지 않고 오류가 없다고 주장함

부분 구현이면 지원 범위와 미지원 범위를 명시한다.

### 범위를 몰래 넓히지 않는다

요청과 직접 관련 없는 전면 리팩터링, 대량 이름 변경과 파일 이동을 함께 하지 않는다.

큰 리팩터링이 필요하면 이유와 경계를 분리해 별도 작업으로 제안한다.

---

## 6. 아키텍처 원칙

### 서버 권한

서버는 다음의 최종 권한을 가진다.

- 캐릭터 영구 상태
- HP, 자원, 주문 슬롯과 충전
- 인벤토리와 장비
- 확정 토큰 위치와 이동 비용
- 턴과 행동 경제
- 공격, 내성 굴림, 피해와 회복
- 상태 효과와 지속시간
- 캐릭터 생성과 레벨업 확정
- 출처 팩 사용 가능 여부
- 소유권과 권한
- 저장

클라이언트가 보낸 수치, 성공 여부, 피해량, 위치와 소유권을 그대로 신뢰하지 않는다.

클라이언트는 입력, 카메라, 미리보기, UI와 승인된 결과의 부드러운 연출을 담당한다.

### 책임 분리

다음 책임을 가능한 한 분리한다.

- 입력과 카메라
- UI 표현
- 네트워크 명령
- 규칙 정의
- 캐릭터 영구 데이터
- 파생 능력치 계산
- 전투 런타임 상태
- 행동 실행
- 저장과 마이그레이션
- VFX, SFX와 애니메이션
- 번역
- 진단과 로그

하나의 거대한 관리자나 컨트롤러가 모든 책임을 처리하게 하지 않는다.

### 공통 엔진과 전용 콘텐츠

피해, 회복, 굴림, 자원, 대상 지정, 범위, 이동, 상태, 지속시간과 휴식은 공통 엔진을 사용한다.

콘텐츠 고유 예외는 전용 실행 모듈에 둔다.

모든 콘텐츠를 하나의 거대한 선언형 데이터와 조건문에 강제로 넣지 않는다. 반대로 같은 규칙을 콘텐츠마다 복사하지 않는다.

### 고정 ID와 레지스트리

직업, 하위직업, 특성, 주문, 아이템과 상태는 언어와 무관한 고정 ID로 조회한다.

```text
class.fighter
subclass.fighter.champion
feature.fighter.action_surge
spell.fireball
condition.prone
item.weapon.longbow
```

출처 팩 사이에서 임의의 파일 경로를 직접 참조하지 않는다. 콘텐츠 레지스트리와 명시된 의존성을 사용한다.

---

## 7. 클린코드 규약

### 하나의 중심 책임

모듈, 클래스와 서비스는 이름으로 설명할 수 있는 하나의 중심 책임을 가진다.

함수가 여러 단계의 서로 다른 일을 수행하면 작은 명명된 함수나 서비스로 분리한다.

### 규칙 계산을 순수하게 유지

명중 보너스, 주문 DC, 이동 비용과 피해식은 가능한 한 Roblox 인스턴스와 UI에 의존하지 않는 계산 함수로 만든다.

동일한 입력에는 동일한 결과가 나와야 하며 단위 테스트가 가능해야 한다.

### 명시적인 의존성

- 숨겨진 전역 상태를 사용하지 않는다.
- 싱글턴과 서비스 로케이터를 남용하지 않는다.
- 필요한 의존성은 생성자나 함수 인수로 전달한다.
- 순환 의존성을 만들지 않는다.
- 모듈 로딩 순서에 우연히 의존하지 않는다.

### 타입과 계약

- 공개 함수의 입력과 반환 타입을 명시한다.
- 직렬화 데이터와 런타임 객체 타입을 구분한다.
- Remote 입력은 스키마 검증을 거친다.
- `any`와 무분별한 타입 단언을 피한다.
- nil 가능성과 실패 결과를 호출자가 처리하게 한다.
- 가능한 모듈은 Luau strict 타입 검사를 목표로 한다.

### 이름과 상수

- 한국어 표시 문자열을 코드 식별자로 사용하지 않는다.
- 콘텐츠 ID, 오류 코드와 이벤트 이름은 중앙 정의를 사용한다.
- 의미를 알 수 없는 숫자와 문자열을 직접 넣지 않는다.
- 이름에 역할, 단위와 상태를 드러낸다.
- 주석으로 나쁜 구조를 설명하기보다 구조를 개선한다.

### 중복과 추상화

명확하게 반복되는 규칙은 공통 계약으로 추출한다.

아직 반복되지 않은 가능성만 보고 범용 프레임워크를 미리 만들지 않는다.

---

## 8. 성능 규약

### 측정 우선

- CPU, 프레임 시간, 메모리, 네트워크와 인스턴스 수를 측정한다.
- 병목을 프로파일링한 뒤 수정한다.
- 최적화 전후 결과를 비교한다.
- 측정하지 않은 성능 개선을 사실처럼 보고하지 않는다.

### 금지되는 패턴

- 모든 토큰과 오브젝트를 매 프레임 순회
- 모든 격자 셀에 Part, Script 또는 이벤트 생성
- 상태 변화가 없는데 반복 폴링
- 매 프레임 대량 Raycast와 경로 탐색
- 닫힌 UI의 지속 갱신
- 동일한 전체 상태의 반복 복제
- 연결, 태스크와 인스턴스를 해제하지 않음

### 우선하는 패턴

- 이벤트 기반 갱신
- 공간 인덱스와 영역 질의
- 변경된 값만 보내는 델타 동기화
- 가시성과 거리 기반 활성화
- Streaming, culling과 LOD
- 오브젝트 풀링
- 캐시와 명확한 무효화
- 정적 내비게이션 데이터 사전 계산
- 화면에 필요한 격자만 시각화

### 이동 시스템

- 5피트 격자는 논리 데이터로 관리한다.
- 클릭과 WASD는 같은 이동 파이프라인을 사용한다.
- 정적 이동 가능 표면은 씬 게시 시 계산한다.
- 문과 장애물 같은 동적 변경만 런타임에 갱신한다.
- 목적지와 이동 가능 상태가 변할 때만 경로를 재계산한다.

### 성능 예산

기준 장면과 기준 기기를 정한 뒤 다음을 관리한다.

- 클라이언트 프레임 시간
- 서버 스크립트 시간
- 메모리와 증가 추세
- 인스턴스 수
- 네트워크 송수신량
- 경로 탐색 시간
- 씬 로딩과 저장 시간

초기 데스크톱 목표는 부드러운 60 FPS다. 정확한 하한과 장면 규모는 별도 성능 예산 문서에서 확정한다.

---

## 9. 오류 처리와 진단

### 오류를 삼키지 않는다

빈 `pcall`로 오류를 숨기지 않는다.

- 복구 가능한 실패는 구조화된 실패 결과로 반환한다.
- 예상하지 못한 오류는 컨텍스트와 함께 기록한다.
- 사용자에게는 이해 가능한 메시지를 표시한다.
- 부분 적용된 상태는 롤백하거나 안전 상태로 전환한다.

### 구조화된 로그

오류와 중요한 상태 변경에는 가능한 범위에서 다음을 포함한다.

- 오류 코드
- 모듈과 기능
- actorId 또는 tokenId
- contentId
- sourcePack과 version
- campaignId, sceneId와 sessionId
- 플레이 모드와 턴
- 요청 플레이어
- 명령 또는 실행 ID

민감 정보와 전체 저장 데이터를 불필요하게 기록하지 않는다.

### 오류 격리

하나의 주문, 특성, 몬스터 능력 또는 VFX 오류가 전체 세션과 전투 루프를 중단시키지 않게 한다.

- 출처 팩 로딩 시 콘텐츠를 검증한다.
- 잘못된 콘텐츠는 비활성화하고 원인을 표시한다.
- 실행 실패 시 해당 실행을 안전하게 취소한다.
- 자원 소비와 상태 적용은 원자적으로 처리하거나 되돌린다.

### 타임아웃과 취소

데이터 로딩, 저장, Remote 응답과 비동기 작업을 무한히 기다리지 않는다.

씬 전환과 세션 종료 시 진행 중인 작업을 정리한다.

---

## 10. 캐릭터와 시트

### 시트가 원본이다

캐릭터 영구 선택과 현재 상태를 여러 UI와 토큰에 중복 저장하지 않는다.

다음 값은 원본 데이터에서 계산한다.

- 최대 HP
- AC
- 이동속도
- 숙련 보너스
- 기술과 내성
- 공격 보너스와 피해
- 주문 공격과 주문 내성 DC
- 사용 가능한 행동, 주문과 반응
- 최대 자원과 회복 조건

파생 값을 캐시할 수 있지만 언제든 원본에서 재계산할 수 있어야 한다.

### 영구와 런타임 상태 분리

영구 상태:

- 종
- 배경
- 직업 레벨
- 하위직업
- 능력치
- 재주와 선택 특성
- 습득 주문
- 소유 장비

런타임 상태:

- 현재 HP와 임시 HP
- 남은 주문 슬롯
- 사용한 자원
- 장비 활성 상태
- 집중
- 상태 효과
- 턴당 사용 여부

### 레벨업은 원자적이다

1. 현재 상태에서 초안 생성
2. 모든 선택과 조건 검증
3. 변경 결과 미리보기
4. 사용자 확정
5. 서버 재검증
6. 하나의 작업으로 저장

취소와 실패 시 이전 상태를 유지한다.

---

## 11. 규칙 콘텐츠

직업, 하위직업, 특성, 주문, 종, 배경, 재주, 아이템과 몬스터 능력은 필요한 범위에서 다음을 가진다.

- 고정 콘텐츠 ID
- 규칙 세트와 출처 팩
- 획득 조건
- 번역 키
- 사용 시점과 행동 종류
- 대상과 사거리
- 자원과 사용 횟수
- 판정과 효과
- 지속시간과 종료 조건
- 중첩과 충돌
- 회복과 재충전
- UI 표시 조건
- 로그와 연출 이벤트
- DM 판정 지점

구현 수준을 명시한다.

- `Executable`: 결과까지 시스템 처리
- `Guided`: 절차와 추적을 제공하고 사용자 또는 DM이 선택
- `Assisted`: 도구와 기록을 제공하고 DM이 결과 확정

어려운 규칙을 몰래 단순화해 `Executable`이라고 표시하지 않는다.

---

## 12. 현지화

- 기본 로케일은 `ko-KR`다.
- 코드에 사용자용 한국어 문구를 직접 넣지 않는다.
- 이름과 설명은 번역 키로 표시한다.
- 규칙 수치와 실행 로직을 번역 파일에 넣지 않는다.
- UI는 다른 언어의 길이 변화를 견뎌야 한다.
- 로그 문장은 번역 템플릿과 변수로 구성한다.
- 번역 누락은 개발 환경에서 즉시 표시하고 기록한다.

---

## 13. 저장과 마이그레이션

- 모든 저장 스키마는 버전을 가진다.
- 스키마 변경에는 마이그레이션이 필요하다.
- 마이그레이션은 반복 실행해도 데이터를 중복 변경하지 않게 한다.
- 레벨업, 장비 변경과 보상은 부분 적용되지 않게 한다.
- 중복 요청이 중복 보상과 자원 소비를 만들지 않게 한다.
- 재접속 시 서버 권위 상태에서 복구한다.
- 저장 실패를 성공으로 표시하지 않는다.
- 자동 저장과 명시적 저장의 충돌을 제어한다.

---

## 14. 네트워크와 보안

모든 Remote 요청에서 확인한다.

- 호출 권한
- 토큰 소유권
- 입력 타입과 크기
- 현재 세션과 플레이 모드
- 거리, 시야와 대상 유효성
- 행동, 턴과 자원
- 요청 빈도
- 중복 실행 ID

클라이언트가 전달한 인스턴스와 ID가 현재 캠페인과 씬에 속하는지 검증한다.

신뢰할 수 없는 사용자 문자열을 로그, 번역 키와 콘텐츠 경로에 직접 사용하지 않는다.

---

## 15. 테스트와 품질 게이트

변경 성격에 따라 다음을 확인한다.

- 정상 사용 흐름
- 권한 없는 요청
- 잘못된 대상과 범위
- 자원 부족
- 중복 요청
- 네트워크 지연과 순서 변경
- 플레이어 이탈과 재접속
- 씬 전환과 세션 종료
- 저장 실패와 마이그레이션
- 다수 토큰과 큰 장면 성능
- 장시간 실행 후 메모리 증가

자동화를 우선할 영역:

- 규칙 계산
- 파생 능력치
- 경로 비용
- 콘텐츠 정의 검증
- 캐릭터 생성과 레벨업
- 직렬화와 마이그레이션
- 상태 효과 수명
- 행동 경제

프로덕션 경로에서 테스트용 가짜 데이터와 우회 로직을 남기지 않는다.

### 테스트 상태를 항상 추적한다

루트 `AGENT-TEST-STATUS.md`는 모든 에이전트가 보는 현재 테스트 상태 대시보드다.

- 구현·Runtime·Acceptance·Release 작업을 시작하기 전에 읽는다.
- 테스트를 실제 수행했거나 PASS/FAIL/BLOCKED/DEFERRED 상태, 다음 Gate, 테스트 가능 범위가 바뀌면 같은 작업에서 갱신한다.
- 사용자가 직접 수행해야 하는 Runtime 테스트는 작은 수정마다 쪼개지 않고 Batch Acceptance로 묶는다.
- 현재 사용자 Runtime 실행 그룹, 완료 수, 다음 수동 체크리스트가 한눈에 보여야 한다.
- 세부 테스트 계약의 원본은 `implementation/roblox/EXECUTION-TEST-RULES.md`, `implementation/roblox/GRAND-ACCEPTANCE-CAMPAIGN.md`, `implementation/roblox/grand-acceptance-manifest.json`이다. 상태 대시보드가 이 원본을 임의로 재정의하면 안 된다.
- Static·Build·Lint·Type PASS를 Studio Runtime PASS로 확대하지 않는다.
- Single-client PASS를 Multi-client·Persistence·Accessibility·Performance·Release PASS로 확대하지 않는다.
- 실행하지 않은 테스트는 체크하거나 PASS로 기록하지 않는다.

현재 운영 기본값은 Codex를 코드·문서 검수, 구조·권한·회귀 검수, Static Gate처럼 토큰·시간 대비 이득이 큰 작업에 사용하고, Roblox Studio 실제 화면·입력·Play·UI·UX 확인은 사용자 Batch Acceptance로 수행하는 것이다. Codex ↔ Studio MCP 자동화는 사용자가 다시 명시적으로 요청하거나 반복 자동화 이득이 명확할 때만 사용한다.

---

## 16. 완료 조건

작업은 다음이 확인되어야 완료다.

1. 기획과 ADR 요구 충족
2. 서버 권한과 입력 검증
3. 정상 흐름과 주요 실패 흐름 확인
4. 저장과 재접속 영향 확인
5. 한국어 번역 키 준비
6. 프로파일링에서 심각한 병목 없음
7. 이벤트, 태스크와 인스턴스 정리
8. 오류 원인을 찾을 로그와 컨텍스트 존재
9. 코드 책임과 의존성이 설명 가능
10. 남은 제한과 미지원 범위 명시

실행할 수 없는 테스트는 실행한 것처럼 보고하지 않는다.

---

## 17. 작업 완료 보고

완료 보고에는 다음을 포함한다.

1. 변경한 목표
2. 변경 파일과 핵심 구조
3. 검증하거나 실행한 테스트
4. 성능 확인 결과 또는 미측정 항목
5. 남아 있는 제한, 위험과 다음 작업

테스트 상태가 바뀐 작업이라면 완료 보고 전에 `AGENT-TEST-STATUS.md`가 같은 상태를 반영하는지 확인한다.

실패한 작업을 성공한 것처럼 표현하지 않는다.

---

## 18. 금지 사항

- 서버 검증 생략
- 빈 `pcall`과 오류 무시
- 거대한 관리자에 책임 계속 추가
- 같은 규칙 코드 복사
- 무제한 루프, 연결과 태스크 방치
- 모든 격자 셀의 물리 인스턴스화
- 전체 장면 매 프레임 검색
- 한국어 문자열을 내부 식별자로 사용
- 저장 스키마를 마이그레이션 없이 변경
- 성능 측정 없이 최적화됐다고 주장
- 테스트 없이 오류가 없다고 주장
- 미완성 기능을 정상 기능처럼 노출
- 확정 문서와 다른 결정을 구현에 몰래 포함