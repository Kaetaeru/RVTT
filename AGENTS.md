# RVTT Agent Rules

이 문서는 RVTT 저장소에서 작업하는 모든 AI 에이전트와 자동화 도구가 따라야 하는 최상위 작업 규약이다.

목표는 에이전트가 많은 코드를 빠르게 만드는 것이 아니라, 합의된 방향을 잃지 않고 검증 가능한 단위로 기획과 구현을 진행하게 하는 것이다.

---

## 1. 프로젝트의 고정 전제

아래 항목은 별도의 새 결정이 있기 전까지 변경하지 않는다.

1. RVTT는 Roblox에서 D&D 세션을 진행하기 위한 게임형 VTT다.
2. 기본 규칙 세트는 `dnd5e-2024`다.
3. 기본 표시 언어는 한국어 `ko-KR`다.
4. 내부 ID, 저장 데이터, 실행 로직은 표시 언어와 분리한다.
5. 캐릭터 시트는 캐릭터 영구 상태와 성장 기록의 원본이다.
6. 캐릭터 생성, 1–20레벨 성장, 직업, 하위직업, 주문, 장비와 전투 기능을 하나의 시스템으로 연결한다.
7. 마법과 특성은 플레이어가 조립하는 범용 프리셋이 아니라 RVTT가 직접 구현하는 정식 콘텐츠다.
8. 추가 공식 서적은 개발자 관리형 출처 팩으로 추가한다.
9. 중요한 게임 상태와 판정의 최종 권한은 서버가 가진다.
10. 완전 자동화가 부적절한 규칙은 DM 승인과 판정 보조 흐름으로 구현한다.

관련 결정은 `docs/remake/decisions/`의 ADR을 기준으로 한다.

---

## 2. 지시와 문서의 우선순위

내용이 충돌할 때 다음 순서를 따른다.

1. 사용자가 현재 작업에서 명시적으로 내린 최신 결정
2. 상태가 `확정`인 ADR
3. 승인된 상세 기획 문서
4. 현재 이슈 또는 작업 명세
5. 이 `AGENTS.md`
6. 기존 코드의 관행

이 우선순위는 제품 결정과 저장소 내부 규약에 적용한다. 플랫폼의 시스템 지시, 보안 정책과 도구 제한을 무시하라는 뜻이 아니다.

기존 코드가 확정된 문서와 충돌하면 기존 코드를 유지하지 말고 충돌을 보고한다.

확정 문서끼리 충돌하거나 사용자의 의도가 불분명하면 임의로 중대한 제품 결정을 만들지 않는다. 가능한 범위까지만 진행하고 충돌 지점을 명시한다.

---

## 3. 기획 작업과 구현 작업을 분리한다

### 기획 작업

기획 요청에서는 다음을 수행한다.

- 목표와 비목표를 구분한다.
- 사용자 흐름과 규칙 동작을 먼저 정의한다.
- 확정, 제안, 보류를 명확히 표시한다.
- 큰 결정은 ADR로 기록한다.
- 구현 전에 필요한 데이터, 상태, 예외와 완료 조건을 적는다.

기획만 요청받았는데 추측으로 프로덕션 코드를 만들지 않는다.

현재 `planning/rvtt-remake` 브랜치는 리메이크 기획을 누적하는 브랜치다. 사용자가 구현을 명시적으로 요청하지 않았다면 이 브랜치에서 프로덕션 시스템 구현을 시작하지 않는다.

### 구현 작업

구현 요청에서는 다음을 수행한다.

- 관련 기획과 ADR을 먼저 읽는다.
- 현재 코드와 데이터 구조를 조사한다.
- 한 번에 하나의 응집된 기능을 끝까지 연결한다.
- 기능에 필요한 서버, 클라이언트, 데이터, UI, 번역, 저장과 검증을 함께 고려한다.

문서에 없는 중대한 규칙을 구현 과정에서 몰래 확정하지 않는다.

---

## 4. 작업을 시작하기 전 절차

모든 작업은 아래 순서로 시작한다.

1. 이 `AGENTS.md`를 읽는다.
2. `docs/remake/README.md`와 관련 ADR을 읽는다.
3. 변경 대상과 연결된 기존 파일을 조사한다.
4. 같은 책임을 가진 기존 모듈, ID, 번역 키와 데이터 구조가 있는지 검색한다.
5. 작업 범위와 완료 조건을 짧게 정의한다.
6. 불필요한 리팩터링 없이 가장 작은 응집 단위를 선택한다.

파일명이나 모듈 위치를 추측한 채 새 중복 구조를 만들지 않는다.

현재 브랜치에 다른 사람이 만든 변경이 있으면 함부로 되돌리거나 덮어쓰지 않는다.

---

## 5. 작업 단위 원칙

### 세로로 완성한다

기능을 여러 개 얕게 만드는 대신 기능 하나를 실제 사용 가능한 상태까지 완성한다.

예를 들어 `행동 연쇄`를 구현한다면 다음을 함께 확인한다.

- 캐릭터가 해당 특성을 획득하는 과정
- 시트와 행동 UI 표시
- 사용 가능 조건
- 자원 소비와 회복
- 서버 검증
- 실제 행동 경제 변경
- 전투 로그
- 한국어 번역
- 저장과 재접속
- 테스트 또는 검증 절차

### 미완성을 숨기지 않는다

다음 상태를 완성으로 표시하지 않는다.

- 버튼만 있고 실행되지 않음
- 설명만 있고 규칙 효과가 없음
- 클라이언트 연출만 있고 서버 상태가 바뀌지 않음
- 저장되지 않음
- 한국어 키가 누락됨
- 정상 경로만 작동하고 잘못된 요청을 검증하지 않음
- 임시 상수와 가짜 데이터에 의존함

부분 구현이면 지원 범위와 미지원 범위를 명시한다.

### 작업 범위를 몰래 넓히지 않는다

요청과 직접 관련 없는 대규모 파일 이동, 이름 변경, 포맷 변경, 전면 리팩터링을 함께 하지 않는다.

필요한 리팩터링이 크다면 별도 작업으로 분리한다.

---

## 6. 아키텍처 원칙

### 서버 권한

서버는 다음 항목의 최종 권한을 가진다.

- 캐릭터 영구 상태
- HP, 자원, 주문 슬롯과 충전
- 인벤토리와 장비
- 토큰의 확정 위치와 이동 결과
- 턴과 행동 경제
- 공격, 내성 굴림, 피해와 회복 결과
- 상태 효과와 지속시간
- 캐릭터 생성과 레벨업 확정
- 출처 팩 사용 가능 여부
- 권한과 소유권

클라이언트가 보내는 수치, 대상, 성공 여부, 피해량, 소유권을 그대로 신뢰하지 않는다.

클라이언트는 입력, 미리보기, 로컬 연출과 서버가 승인한 상태의 표시를 담당한다.

### 책임 분리

다음 책임을 가능한 한 분리한다.

- 규칙 정의
- 캐릭터 영구 데이터
- 파생 능력치 계산
- 런타임 전투 상태
- 행동 실행과 판정
- 저장과 마이그레이션
- UI 표현
- VFX, SFX와 애니메이션
- 번역

하나의 거대한 컨트롤러가 데이터, 규칙, UI, 저장과 네트워크를 모두 담당하게 만들지 않는다.

### 공통 엔진과 전용 콘텐츠

피해, 회복, 굴림, 자원, 대상 지정, 범위, 지속시간, 상태, 이동과 휴식 같은 반복 규칙은 공통 엔진을 사용한다.

각 마법과 특성의 고유한 예외는 콘텐츠 전용 모듈로 구현할 수 있다.

모든 콘텐츠를 거대한 선언형 데이터만으로 강제하지 않는다. 반대로 같은 규칙 코드를 콘텐츠마다 복사하지 않는다.

### 직접 참조 대신 레지스트리

직업, 특성, 주문, 아이템과 상태는 고정 콘텐츠 ID를 통해 조회한다.

예시:

```text
class.fighter
subclass.fighter.champion
feature.fighter.action_surge
spell.fireball
condition.prone
item.weapon.longbow
```

출처 팩 사이에서 임의의 파일 경로를 직접 참조하기보다 콘텐츠 레지스트리와 명시된 의존성을 사용한다.

---

## 7. 캐릭터와 시트 규칙

### 시트가 원본이다

캐릭터의 영구 선택과 현재 상태를 서로 다른 UI와 토큰에 중복 저장하지 않는다.

다음 값은 가능한 한 원본 데이터에서 계산한다.

- 최대 HP
- AC
- 이동속도
- 숙련 보너스
- 기술과 내성 보너스
- 무기 공격 보너스와 피해
- 주문 공격 보너스와 주문 내성 DC
- 사용 가능한 행동, 주문과 반응
- 최대 자원과 회복 조건

파생 값을 캐시할 수는 있지만, 원본처럼 취급하지 않는다. 캐시는 재계산 가능해야 한다.

### 영구 상태와 런타임 상태를 구분한다

영구 상태 예시:

- 종
- 배경
- 직업 레벨
- 하위직업
- 능력치
- 선택한 재주와 특성
- 습득한 주문
- 소유 장비

런타임 상태 예시:

- 현재 HP와 임시 HP
- 남은 주문 슬롯
- 사용한 직업 자원
- 현재 장비 상태
- 집중 중인 효과
- 상태이상
- 턴당 사용 여부

두 상태를 하나의 무분별한 테이블에 섞지 않는다.

### 레벨업은 원자적 작업이다

레벨업 결과를 선택 과정 중에 캐릭터에 부분 적용하지 않는다.

1. 현재 상태에서 레벨업 초안을 만든다.
2. 모든 선택과 조건을 검증한다.
3. 변경 결과를 미리 계산한다.
4. 사용자가 확정한다.
5. 서버가 다시 검증한다.
6. 하나의 트랜잭션으로 저장한다.

취소나 실패 시 이전 캐릭터 상태가 유지되어야 한다.

---

## 8. 규칙 콘텐츠 구현 규약

모든 직업, 하위직업, 특성, 주문, 종, 배경, 재주, 아이템과 몬스터 능력은 필요한 범위에서 다음 정보를 가져야 한다.

- 고정 콘텐츠 ID
- 소속 규칙 세트와 출처 팩
- 획득 조건
- 표시용 번역 키
- 사용 시점과 행동 종류
- 대상 지정 방식
- 사거리와 시야 조건
- 자원과 사용 횟수
- 판정 방식
- 적용 효과
- 지속시간과 종료 조건
- 중첩과 충돌 규칙
- 휴식 또는 재충전 규칙
- UI 표시 조건
- 로그와 연출 이벤트
- DM 승인 또는 수동 처리 지점

### 구현 수준을 명시한다

콘텐츠는 다음 중 하나로 분류한다.

- `Executable`: 선택부터 결과 적용까지 시스템이 처리
- `Guided`: 시스템이 절차와 추적을 제공하고 사용자 또는 DM이 의미 있는 선택을 수행
- `Assisted`: 자유도가 높은 규칙에 전용 도구와 기록을 제공하고 DM이 결과 확정

어려운 규칙을 몰래 단순화하여 `Executable`이라고 표시하지 않는다.

### 규칙 원문과 구현 해석을 구분한다

설명문, 구현 메모, 예외 처리와 RVTT의 해석을 하나의 필드에 섞지 않는다.

규칙이 애매한 경우 코드에 임의 해석을 숨기지 말고 문서 또는 ADR에 해석 근거를 남긴다.

---

## 9. 행동 실행 파이프라인

행동, 주문과 특성은 공통적으로 다음 흐름을 따른다.

1. 클라이언트가 콘텐츠 ID와 사용 의도를 전송한다.
2. 서버가 사용자 권한과 캐릭터 소유권을 확인한다.
3. 서버가 캐릭터가 해당 콘텐츠를 실제로 보유하는지 확인한다.
4. 현재 모드, 턴, 행동 경제와 반응 가능 여부를 확인한다.
5. 자원, 장비, 상태, 집중과 재사용 조건을 확인한다.
6. 대상, 거리, 시야, 엄폐와 대상 유형을 검증한다.
7. 실행에 필요한 선택을 확정한다.
8. 서버가 판정과 효과를 계산한다.
9. 게임 상태를 원자적으로 적용하고 필요한 자원을 소비한다.
10. 구조화된 결과 이벤트와 전투 로그를 생성한다.
11. 클라이언트가 승인된 결과에 맞춰 UI와 연출을 재생한다.

검증 실패 시 실패 이유를 구조화하여 반환한다. 클라이언트에서만 막고 서버 검증을 생략하지 않는다.

여러 대상이나 여러 효과가 있는 행동은 중간 실패로 상태가 반만 적용되지 않도록 실행 단위를 명확히 한다.

---

## 10. 출처 팩 규약

추가 공식 서적은 출처 팩으로 구현한다.

각 팩은 최소한 다음 메타데이터를 가진다.

```text
packId
version
rulesetId
sourceId
dependencies
loadOrder
contentIndex
locales
```

규칙:

1. `packId`와 콘텐츠 ID는 한번 공개된 뒤 함부로 변경하지 않는다.
2. 표시 이름을 ID로 사용하지 않는다.
3. 의존성은 명시한다.
4. 순환 의존성을 만들지 않는다.
5. 다른 팩의 콘텐츠를 덮어쓸 때는 명시적 패치 또는 확장 규칙을 사용한다.
6. 캠페인에 활성화되지 않은 팩의 콘텐츠는 생성과 레벨업 후보에 노출하지 않는다.
7. 사용 중인 팩을 비활성화할 때 참조 무결성을 검사한다.
8. 팩 버전 변경으로 저장 데이터가 달라지면 마이그레이션을 제공한다.
9. 팩 제거를 조용히 데이터 삭제로 처리하지 않는다.

---

## 11. 현지화 규약

기본 로케일은 `ko-KR`이다.

### 금지

- UI 코드에 사용자 표시용 한국어 문장을 직접 작성
- 한국어 이름을 콘텐츠 ID나 저장 키로 사용
- 번역된 설명문에 규칙 수치를 파싱 의존
- 글자가 박힌 이미지만으로 기능 이름 전달
- 문자열 이어 붙이기로 전투 로그 문장 생성

### 필수

- 모든 사용자 표시 문자열에 번역 키 사용
- 이름, 짧은 이름, 설명, 선택지와 오류 메시지 분리
- 로그는 번역 템플릿과 구조화된 변수로 생성
- 숫자, 단위와 문법 처리를 로케일 계층에 둠
- 누락된 키에 대한 fallback과 개발 경고 제공
- 다른 언어 길이를 견딜 수 있는 UI 레이아웃 사용

기능을 완료했다고 판단하려면 기본 한국어 리소스가 함께 존재해야 한다.

---

## 12. 저장과 버전 관리 규약

저장 가능한 주요 데이터는 스키마 버전을 가진다.

예시:

```text
schemaVersion
rulesetId
enabledSourcePacks
characterVersion
```

규칙:

1. 저장 스키마 변경 시 마이그레이션 경로를 만든다.
2. 필드 삭제나 의미 변경을 조용히 수행하지 않는다.
3. 콘텐츠는 고정 ID로 참조한다.
4. 출처 팩 버전과 필요한 의존성을 저장한다.
5. 저장 실패 시 성공한 것처럼 UI를 갱신하지 않는다.
6. 재시도와 중복 요청이 데이터를 두 번 적용하지 않도록 고려한다.
7. 캐릭터 생성, 레벨업, 아이템 이동 같은 중요한 변경은 원자성을 우선한다.
8. DataStore에 Roblox Instance, 함수 또는 직렬화할 수 없는 값을 저장하지 않는다.
9. 삭제 작업은 복구 또는 명시적 확인 없이 자동 실행하지 않는다.
10. 로드 시 유효하지 않은 참조를 발견하면 조용히 제거하지 말고 오류 상태와 복구 경로를 제공한다.

---

## 13. Roblox와 Luau 코드 규약

새 Luau 모듈은 가능한 한 `--!strict`를 사용한다.

### 기본 규칙

- 전역 변수를 만들지 않는다.
- 서비스는 `game:GetService()`로 명시적으로 가져온다.
- 공개 함수와 데이터 경계에는 타입을 작성한다.
- 하나의 모듈은 한 가지 주요 책임을 가진다.
- 반복 숫자와 문자열은 의미 있는 상수 또는 정의로 분리한다.
- 실패 가능한 작업은 명시적으로 결과를 반환하거나 오류를 처리한다.
- 연결한 이벤트, 태스크와 임시 Instance의 수명과 정리를 관리한다.
- 이름만 다른 중복 모듈을 만들지 않는다.
- 기존 공개 API를 변경할 때 호출부와 저장 호환성을 함께 조사한다.

### Remote 규칙

모든 Remote 요청은 서버에서 다음을 검증한다.

- 호출자 권한
- 소유권
- 요청 빈도
- 입력 타입과 범위
- 현재 게임 상태
- 콘텐츠 보유 여부
- 대상과 거리
- 자원과 행동 경제

클라이언트가 전달한 Instance 경로, 피해량, 주사위 결과와 권한을 신뢰하지 않는다.

### 성능 규칙

- 필요 없는 매 프레임 전체 탐색을 피한다.
- 큰 컬렉션을 매번 재생성하지 않는다.
- 이벤트 기반으로 처리할 수 있는 상태를 폴링하지 않는다.
- 클라이언트마다 불필요하게 전체 비밀 데이터를 복제하지 않는다.
- 성능 최적화를 이유로 권한 검증과 데이터 무결성을 제거하지 않는다.

---

## 14. UI와 사용자 경험 규약

1. 플레이, 전투, 장면 편집과 캐릭터 관리 모드를 명확히 구분한다.
2. 현재 선택된 캐릭터, 행동, 대상과 비용을 항상 알 수 있게 한다.
3. 사용할 수 없는 기능은 이유를 제공한다.
4. 서버 응답 전 성공한 것처럼 확정 표시하지 않는다.
5. 취소, 되돌리기와 DM 수정이 필요한 흐름을 고려한다.
6. 빈 버튼, 작동하지 않는 메뉴와 가짜 진행 상태를 남기지 않는다.
7. 핵심 기능을 툴팁에만 숨기지 않는다.
8. 키보드와 마우스를 기본으로 하되 입력 체계는 UI에 하드코딩하지 않는다.
9. 한국어를 기준으로 검수하되 다른 언어에서도 잘리지 않게 만든다.
10. 오류는 개발자 로그에만 남기지 말고 사용자에게 필요한 수준의 피드백을 제공한다.

---

## 15. 테스트와 검증 규약

기능 변경 후 가능한 검증을 실제로 수행한다.

저장소에 테스트, 린트, 타입 검사 또는 빌드 명령이 있으면 관련 명령을 실행한다. 존재하지 않는 검증 도구를 실행했다고 주장하지 않는다.

최소 검증 항목:

- 정상 사용
- 조건 미충족 사용
- 권한 없는 사용
- 잘못된 대상
- 자원 부족
- 중복 요청
- 턴 또는 모드가 다른 상태
- 저장 후 재접속
- 출처 팩 비활성 상태
- 번역 키 누락
- 서버와 클라이언트 상태 불일치 복구

규칙 콘텐츠는 가능한 경우 다음 경계를 확인한다.

- 획득 직전과 직후 레벨
- 최소와 최대 대상 수
- 사거리 경계
- 피해와 회복의 최소·최대 또는 치명타
- 턴 시작과 종료
- 짧은 휴식과 긴 휴식
- 집중 종료와 효과 제거
- 다중직업 상호작용

검증하지 못한 항목은 완료 보고에 명시한다.

---

## 16. 문서화 규약

다음 변경은 문서 갱신이 필요하다.

- 제품 방향 또는 비목표 변경
- 규칙판과 출처 정책 변경
- 저장 스키마 변경
- 공개 모듈 API 변경
- 새로운 콘텐츠 구현 방식 도입
- 권한 모델 변경
- 캐릭터 생성 또는 레벨업 흐름 변경
- 예외적인 규칙 해석 결정

큰 결정은 ADR로 기록한다.

ADR에는 최소한 다음을 포함한다.

- 상태
- 배경
- 결정
- 이유
- 결과와 제약
- 고려했지만 선택하지 않은 대안

이미 확정된 ADR을 조용히 다시 쓰지 않는다. 결정이 바뀌면 새 ADR에서 이전 결정을 대체한다.

---

## 17. Git 작업 규약

1. `main`에 직접 실험성 변경을 만들지 않는다.
2. 하나의 브랜치와 PR은 가능한 한 하나의 명확한 목적을 가진다.
3. 커밋 메시지는 변경 의도를 드러내게 작성한다.
4. 관련 없는 파일 포맷 변경을 섞지 않는다.
5. 사용자가 요청하지 않은 강제 푸시, 히스토리 재작성, 브랜치 삭제와 PR 병합을 하지 않는다.
6. 비밀키, 토큰, 개인 정보와 로컬 설정을 커밋하지 않는다.
7. 생성 파일과 캐시는 저장소 정책에 맞지 않으면 커밋하지 않는다.
8. 다른 변경을 되돌려야 할 때는 이유와 범위를 먼저 확인한다.

권장 커밋 접두사:

```text
docs:
feat:
fix:
refactor:
test:
chore:
```

---

## 18. 완료 조건

기능은 다음 조건을 만족해야 완료로 간주한다.

- 합의된 요구사항을 충족한다.
- 서버 권한과 입력 검증이 적용된다.
- 시트, 전투 상태와 저장 데이터 사이에 중복 원본이 없다.
- 기본 한국어 UI와 설명이 존재한다.
- 필요한 출처 팩과 콘텐츠 ID가 등록된다.
- 실패와 취소 경로가 처리된다.
- 재접속 또는 재로드 후 상태가 유지된다.
- 관련 문서가 갱신된다.
- 가능한 테스트와 검증을 수행했다.
- 수행하지 못한 검증과 남은 제한을 보고했다.
- 디버그 출력, 임시 우회와 사용하지 않는 코드가 제거된다.

---

## 19. 금지되는 지름길

에이전트는 다음 행동을 하지 않는다.

- 사용자의 결정 없이 규칙판을 섞음
- 한국어 표시 이름을 내부 ID로 사용
- 클라이언트가 계산한 결과를 서버가 그대로 적용
- 모든 콘텐츠를 하나의 거대한 스크립트에 추가
- 같은 로직을 직업과 주문마다 복사
- 완성되지 않은 기능을 UI에 정상 기능처럼 노출
- 저장 스키마를 변경하고 마이그레이션을 생략
- 출처 팩 제거 시 관련 캐릭터 데이터를 조용히 삭제
- 테스트하지 않은 기능을 완전히 검증했다고 주장
- 임시 값과 가짜 데이터에 의존한 구현을 최종 상태로 남김
- 문서와 충돌하는 구현을 이유 설명 없이 유지
- 요청과 무관한 대규모 리팩터링을 함께 수행
- 저작권이 있는 규칙 원문을 저장소에 불필요하게 대량 복제

---

## 20. 작업 완료 보고 형식

에이전트는 작업을 마친 뒤 다음 내용을 간단하고 정확하게 보고한다.

1. 변경한 내용
2. 변경한 주요 파일
3. 수행한 검증
4. 검증하지 못한 항목
5. 남아 있는 제한 또는 다음 작업

성공하지 않은 작업을 성공한 것처럼 표현하지 않는다.
