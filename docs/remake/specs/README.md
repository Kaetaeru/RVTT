# 구현 명세

확정된 기획을 실제 모듈, 타입, 명령, 네트워크, 저장, 오류, 테스트와 성능 계약으로 변환한다.

기획 문서의 `즉시 구현 명세 가능성`이 `READY` 또는 허용 가능한 `READY_WITH_DEFAULTS`일 때만 작성한다.

영역별 명세는 하위 폴더에 3자리 번호와 의미 있는 이름으로 작성한다.

예: `combat/001-encounter-bootstrap.md`